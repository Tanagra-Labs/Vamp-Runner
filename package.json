{
  "name": "vamp-runner",
  "version": "1.1.0",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check game.js && node --check rules.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
