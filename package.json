{
  "name": "vamp-runner",
  "version": "1.5.0",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check game.js && node --check rules.js && node --check levels.js && node --check audio.js && node --check bat.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
